<h1>Books</h1>

<p>
	Here is a list of all the books that have been entered into the system.
</p>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>ISBN</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @books.each do |book| %>
      <tr>
        <td><%= book.title %></td>
        <td><%= book.ISBN %></td>
        <td><%= link_to 'Details', book %></td>
        <td><%= link_to 'Edit', edit_book_path(book) %></td>
        <td><%= link_to 'Delete', book, method: :delete, data: { confirm: 'Are you sure you want to delete this book?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Book', new_book_path %>
